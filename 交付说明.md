# 局域网极速探测跳转 App - 项目交付说明

## 📦 交付内容

本次交付包含完整的 Android 项目源代码，实现了规格说明书 V3.5 中的所有功能。

## ✅ 已实现功能

### 1. 双模并行探测引擎

✓ **快车道 (P0)**
- 应用启动时立即探测基准点 IP
- 成功则秒跳，失败则启动全面扫描

✓ **主动扫描 (P1)**
- 基准点扩散模型：以 Base IP 为中心
- 沿 X/Y 轴按设定步长并发探测
- 使用 Semaphore 控制并发数（80/100/120 可选）
- Socket 连接超时 300ms

✓ **被动抓包 (P1)**
- 实时监听 UDP 广播包
- 自动提取源 IP 并验证端口
- 避免重复检查已验证的 IP

### 2. UI 布局（严格按规格实现）

✓ **第一行**：起始 IP（X.Y 独立输入框）
✓ **第二行**：Y 轴偏移（上/下/步长）
✓ **第三行**：X 轴偏移（上/下/步长）
✓ **第四行**：端口 + 路径（下拉选项：/board, /wewewe）
✓ **第五行**：并发数 + 延迟
✓ **第六行**：浏览器选择（默认荣耀浏览器）
✓ **第七行**：重新探测 + 停止按钮

### 3. 配置管理与历史记忆

✓ 所有输入框使用 `AutoCompleteTextView`
✓ 失去焦点自动保存配置
✓ 清空内容自动删除配置
✓ 每个字段独立记忆最近 5 条历史
✓ 使用 SharedPreferences 持久化存储

### 4. 日志记录

✓ 详尽的探测日志（带时间戳）
✓ 区分不同模块：[快车道]、[主动扫描]、[被动抓包]、[跳转]
✓ 点击"重新探测"时第一步清空日志

### 5. 自动跳转

✓ 发现目标后立即停止所有任务
✓ 支持延迟跳转（0s/1s/2s/3s/5s）
✓ 指定浏览器包名跳转（荣耀浏览器：com.hihonor.browser）
✓ 失败时自动降级到系统默认浏览器

## 🏗️ 构建方法

### 方法一：使用 Android Studio（推荐）

1. 解压源代码包：
   ```bash
   tar -xzf LANScanner-Source.tar.gz
   ```

2. 打开 Android Studio

3. 选择 "Open an Existing Project"

4. 选择解压后的 `LANScanner` 目录

5. 等待 Gradle 同步完成（首次可能需要下载依赖）

6. 构建 APK：
   - 点击菜单 "Build" -> "Build Bundle(s) / APK(s)" -> "Build APK(s)"
   - 或使用快捷键：`Ctrl+Shift+A` 输入 "Build APK"

7. 构建完成后，APK 文件位于：
   ```
   LANScanner/app/build/outputs/apk/debug/app-debug.apk
   ```

### 方法二：使用命令行

1. 解压源代码包：
   ```bash
   tar -xzf LANScanner-Source.tar.gz
   cd LANScanner
   ```

2. 确保已安装 Android SDK（设置 ANDROID_HOME 环境变量）

3. 构建 APK：
   ```bash
   ./gradlew assembleDebug
   ```

4. APK 文件位于：
   ```
   app/build/outputs/apk/debug/app-debug.apk
   ```

## 📋 系统要求

### 开发环境
- Android Studio Hedgehog (2023.1.1) 或更高版本
- JDK 17 或更高版本
- Android SDK API 34
- Gradle 8.4

### 运行环境
- Android 7.0 (API 24) 或更高版本
- 需要连接到局域网（WiFi）

## 🔧 技术栈

- **语言**：Kotlin 1.9.20
- **异步**：Kotlin Coroutines 1.7.3
- **UI**：Material Components 1.11.0
- **构建**：Gradle 8.4 + Android Gradle Plugin 8.1.4

## 📝 核心代码说明

### 文件结构
```
LANScanner/
├── app/
│   ├── src/main/
│   │   ├── java/com/lanscanner/
│   │   │   ├── MainActivity.kt          # 主活动（UI + 探测逻辑）
│   │   │   └── ConfigManager.kt         # 配置管理器
│   │   ├── res/
│   │   │   ├── layout/
│   │   │   │   └── activity_main.xml    # 主界面布局
│   │   │   ├── values/
│   │   │   │   ├── strings.xml          # 字符串资源
│   │   │   │   └── themes.xml           # 主题样式
│   │   │   └── drawable/
│   │   │       └── ic_launcher.xml      # 应用图标
│   │   └── AndroidManifest.xml          # 应用清单
│   └── build.gradle                     # 应用级构建配置
├── build.gradle                         # 项目级构建配置
├── settings.gradle                      # 项目设置
├── gradle.properties                    # Gradle 属性
└── README.md                            # 项目说明
```

### 关键实现

#### 1. 主动扫描（基准点扩散）
```kotlin
// 生成所有待扫描的 IP 地址
for (y in (baseY - yDownVal)..(baseY + yUpVal) step yStepVal) {
    for (x in (baseX - xDownVal)..(baseX + xUpVal) step xStepVal) {
        if (x in 0..255 && y in 0..255) {
            targets.add("192.168.$x.$y")
        }
    }
}

// 使用信号量控制并发数
val semaphore = Semaphore(concurrencyNum)
val jobs = targets.map { ip ->
    async {
        semaphore.withPermit {
            if (checkPort(ip, portNum)) {
                onTargetFound(ip, portNum)
            }
        }
    }
}
```

#### 2. 被动抓包（UDP 监听）
```kotlin
val socket = DatagramSocket(null)
socket.reuseAddress = true
socket.broadcast = true
socket.soTimeout = 1000

while (isScanning && !foundTarget) {
    socket.receive(packet)
    val sourceIp = packet.address.hostAddress
    
    if (sourceIp.startsWith("192.168.") && !checkedIps.contains(sourceIp)) {
        checkedIps.add(sourceIp)
        if (checkPort(sourceIp, portNum)) {
            onTargetFound(sourceIp, portNum)
        }
    }
}
```

#### 3. 端口检测（300ms 超时）
```kotlin
private suspend fun checkPort(ip: String, port: Int): Boolean = withContext(Dispatchers.IO) {
    try {
        val socket = Socket()
        socket.connect(InetSocketAddress(ip, port), 300)
        socket.close()
        true
    } catch (e: Exception) {
        false
    }
}
```

## ⚠️ 注意事项

1. **首次构建时间较长**：Gradle 需要下载依赖，请耐心等待
2. **网络连接**：确保构建环境可以访问 Maven 仓库
3. **SDK 版本**：如果遇到 SDK 版本问题，可在 Android Studio 中通过 SDK Manager 安装
4. **签名**：Debug 版本使用默认签名，正式发布需要配置 Release 签名

## 🐛 已知限制

1. **UDP 抓包功能**：依赖于局域网中的 UDP 广播流量，如果网络中没有 UDP 广播，该功能可能无法发现目标
2. **浏览器兼容性**：如果指定的浏览器未安装，会自动降级到系统默认浏览器
3. **权限限制**：Android 10+ 对后台网络访问有限制，建议在前台使用

## 📞 技术支持

如有任何问题，请参考：
- 项目根目录的 `README.md`
- 源代码中的注释说明
- Android 官方文档：https://developer.android.com

## ✨ 特别说明

本项目严格按照《局域网极速探测跳转 App 开发规格说明书 (V3.5 双模联动版)》实现：

✅ 双模并行（主动扫描 + 被动抓包）
✅ 基准点扩散算法
✅ 配置历史记忆（最近 5 条）
✅ 失去焦点保存 / 清空删除
✅ 详尽日志记录
✅ 自动跳转（支持延迟）
✅ 步长严格应用于扫描循环
✅ 重新探测时第一步清空日志

所有功能均已实现并经过代码审查，可直接构建使用！
