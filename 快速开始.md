# 快速开始指南

## 🚀 5 分钟构建你的第一个 APK

### 步骤 1：准备环境

#### 安装 Android Studio
1. 访问 https://developer.android.com/studio
2. 下载并安装最新版本的 Android Studio
3. 首次启动时，按照向导安装 Android SDK

#### 验证 Java 版本
```bash
java -version
```
确保版本为 17 或更高。如果不是，Android Studio 会自动安装合适的 JDK。

### 步骤 2：导入项目

1. **解压项目**（如果是压缩包）
   ```bash
   tar -xzf LANScanner-Source.tar.gz
   ```

2. **打开 Android Studio**

3. **导入项目**
   - 点击 "Open"
   - 选择 `LANScanner` 文件夹
   - 点击 "OK"

4. **等待同步**
   - Android Studio 会自动下载依赖
   - 首次可能需要 5-10 分钟
   - 底部状态栏显示 "Gradle sync finished" 即完成

### 步骤 3：构建 APK

#### 方式 A：使用菜单（推荐新手）

1. 点击顶部菜单 `Build`
2. 选择 `Build Bundle(s) / APK(s)`
3. 点击 `Build APK(s)`
4. 等待构建完成（通常 1-2 分钟）
5. 右下角会弹出通知，点击 `locate` 查看 APK

#### 方式 B：使用命令行（推荐高级用户）

```bash
cd LANScanner
./gradlew assembleDebug
```

构建完成后，APK 位于：
```
app/build/outputs/apk/debug/app-debug.apk
```

### 步骤 4：安装到手机

#### 方式 A：USB 连接

1. 手机开启"开发者选项"和"USB 调试"
2. USB 连接手机到电脑
3. 在 Android Studio 中点击绿色的 "Run" 按钮
4. 选择你的设备，点击 "OK"

#### 方式 B：直接安装 APK

1. 将 `app-debug.apk` 复制到手机
2. 在手机上点击 APK 文件
3. 允许"安装未知来源应用"
4. 点击"安装"

### 步骤 5：使用应用

1. **首次启动**
   - 应用会自动使用默认配置开始探测
   - 观察日志区域的实时输出

2. **调整配置**
   - 修改起始 IP（例如：192.168.1.1）
   - 设置 Y 轴和 X 轴的扫描范围
   - 调整并发数和延迟

3. **重新探测**
   - 点击"重新探测"按钮
   - 日志会清空并开始新的扫描

4. **停止扫描**
   - 点击"停止"按钮
   - 所有扫描任务立即终止

## 🔧 常见问题

### Q1: Gradle sync 失败怎么办？

**A:** 检查网络连接，确保可以访问 Maven 仓库。如果在国内，可以配置镜像：

在 `settings.gradle` 中添加：
```gradle
maven { url 'https://maven.aliyun.com/repository/public/' }
maven { url 'https://maven.aliyun.com/repository/google/' }
```

### Q2: 构建时提示 SDK 版本不匹配？

**A:** 打开 Android Studio 的 SDK Manager：
1. 点击顶部工具栏的 SDK Manager 图标
2. 安装 API Level 34 (Android 14)
3. 重新同步项目

### Q3: 应用安装后闪退？

**A:** 检查以下几点：
1. 手机 Android 版本是否 ≥ 7.0
2. 是否授予了必要的网络权限
3. 查看 Logcat 日志获取详细错误信息

### Q4: 扫描不到目标设备？

**A:** 确认：
1. 手机和目标设备在同一局域网
2. 目标端口（默认 34832）确实开放
3. 调整扫描范围和步长
4. 增加并发数以提高扫描速度

### Q5: 被动抓包没有反应？

**A:** 被动抓包依赖于局域网中的 UDP 广播流量：
1. 确保网络中有设备发送 UDP 广播
2. 主动扫描会同时进行，不影响整体效果
3. 可以尝试在其他设备上触发网络活动

## 📱 测试建议

### 测试场景 1：快速测试
```
起始 IP: 192.168.1.1
Y 轴: 上 5, 下 5, 步长 1
X 轴: 上 5, 下 5, 步长 1
并发数: 120
```
这将扫描 192.168.0-2.0-6 范围内的所有 IP。

### 测试场景 2：大范围扫描
```
起始 IP: 192.168.1.100
Y 轴: 上 50, 下 50, 步长 1
X 轴: 上 1, 下 1, 步长 1
并发数: 120
```
这将扫描 192.168.0-2.50-150 范围内的所有 IP。

### 测试场景 3：跳跃扫描（提高速度）
```
起始 IP: 192.168.1.1
Y 轴: 上 100, 下 100, 步长 5
X 轴: 上 2, 下 2, 步长 1
并发数: 120
```
使用步长 5 可以快速覆盖更大范围。

## 🎯 性能优化建议

1. **并发数设置**
   - 小范围扫描（<100 个 IP）：80
   - 中等范围（100-500 个 IP）：100
   - 大范围扫描（>500 个 IP）：120

2. **步长设置**
   - 精确扫描：步长 1
   - 快速扫描：步长 3-5
   - 超快扫描：步长 10（可能遗漏目标）

3. **延迟设置**
   - 立即跳转：0s
   - 查看日志后跳转：2-3s
   - 手动确认后跳转：5s

## 📚 进阶使用

### 自定义浏览器

在"浏览器"输入框中输入：
- `Chrome` → 自动匹配 com.android.chrome
- `Firefox` → 自动匹配 org.mozilla.firefox
- `荣耀浏览器` → com.hihonor.browser（默认）

### 历史记录功能

- 每个输入框会记住最近 5 次输入
- 点击输入框即可看到历史记录下拉列表
- 清空输入框内容会删除该字段的配置

### 日志分析

日志格式：`[时间戳] [模块] 消息`

- `[快车道]`：基准点快速探测
- `[主动扫描]`：扩散扫描进度
- `[被动抓包]`：UDP 监听发现
- `[成功]`：目标已锁定
- `[跳转]`：浏览器跳转状态

## 🎉 完成！

现在你已经成功构建并了解了如何使用这个应用。

如果遇到任何问题，请查看：
- 项目根目录的 `README.md`
- `交付说明.md` 中的详细技术说明
- Android 官方文档

祝使用愉快！🚀
